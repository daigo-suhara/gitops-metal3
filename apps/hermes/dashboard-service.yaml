apiVersion: v1
kind: Service
metadata:
  name: hermes-dashboard
  annotations:
    io.cilium/lb-ipam-ips: 172.16.100.132
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: hermes
  ports:
    - name: http
      port: 80
      targetPort: dashboard
