apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openstack-keystone
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "130"
spec:
  project: default
  source:
    repoURL: https://tarballs.opendev.org/openstack/openstack-helm/
    chart: keystone
    targetRevision: "2025.2.20+1bbff8f95"
    helm:
      releaseName: keystone
      values: |
        labels:
          api:
            node_selector_key: openstack-control-plane
            node_selector_value: enabled
          job:
            node_selector_key: openstack-control-plane
            node_selector_value: enabled

        images:
          tags:
            bootstrap: "openstackhelm/heat:wallaby-ubuntu_focal"
            db_init: "openstackhelm/heat:wallaby-ubuntu_focal"
            keystone_db_sync: "openstackhelm/keystone:latest-ubuntu_focal"
            db_drop: "openstackhelm/heat:wallaby-ubuntu_focal"
            ks_user: "openstackhelm/heat:wallaby-ubuntu_focal"
            rabbit_init: "docker.io/library/rabbitmq:3.13-management"
            keystone_fernet_setup: "openstackhelm/keystone:latest-ubuntu_focal"
            keystone_fernet_rotate: "openstackhelm/keystone:latest-ubuntu_focal"
            keystone_credential_setup: "openstackhelm/keystone:latest-ubuntu_focal"
            keystone_credential_rotate: "openstackhelm/keystone:latest-ubuntu_focal"
            keystone_credential_cleanup: "openstackhelm/heat:wallaby-ubuntu_focal"
            keystone_api: "openstackhelm/keystone:latest-ubuntu_focal"
            keystone_domain_manage: "openstackhelm/keystone:latest-ubuntu_focal"
            dep_check: "quay.io/airshipit/kubernetes-entrypoint:latest-ubuntu_focal"
            image_repo_sync: "docker.io/library/docker:17.07.0"

        manifests:
          job_credential_cleanup: false
          test: false

        conf:
          keystone:
            DEFAULT:
              log_config_append: null
            cache:
              backend: oslo_cache.backends.memcache
              enabled: true
              memcache_servers: "memcached.openstack.svc.cluster.local:11211"

        endpoints:
          identity:
            name: keystone
            hosts:
              default: keystone-api
            port:
              api:
                default: 5000
                public: 80
            scheme:
              default: http
              public: http
          oslo_db:
            auth:
              admin:
                password: openstack-changeme
              keystone:
                password: openstack-changeme
          oslo_messaging:
            auth:
              admin:
                username: admin
                password: openstack-changeme
              keystone:
                username: keystone
                password: openstack-changeme

        bootstrap:
          enabled: true
          script: |
            openstack role create --or-show admin
            openstack role create --or-show member
            openstack role create --or-show reader
  destination:
    server: https://172.16.10.10:6443
    namespace: openstack
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
