apiVersion: apps/v1
kind: Deployment
metadata:
  name: baremetal-operator-ironic
  namespace: metal3
spec:
  template:
    spec:
      hostNetwork: true
      securityContext:
        runAsNonRoot: false
        fsGroup: 994
      initContainers:
        - name: image-downloader
          image: alpine:3.20
          securityContext:
            runAsUser: 0
          command: ["sh", "-c"]
          args:
            - |
              set -ex
              apk add --no-cache curl
              DEST=/shared/html/images
              mkdir -p $DEST /shared/tftpboot /shared/ironic /shared/db

              IPA_BASE="https://tarballs.openstack.org/ironic-python-agent/dib/files"
              if [ ! -f $DEST/ironic-python-agent.kernel ]; then
                curl -L -o $DEST/ironic-python-agent.kernel $IPA_BASE/ipa-debian-master.kernel
                curl -L -o $DEST/ironic-python-agent.initramfs $IPA_BASE/ipa-debian-master.initramfs
              fi

              chmod -R 777 /shared
          volumeMounts:
            - name: ironic-data-volume
              mountPath: /shared
      containers:
        # ironic-dnsmasq
        - name: ironic-dnsmasq
          envFrom:
            - configMapRef:
                name: ironic
        # ironic
        - name: ironic
          envFrom:
            - configMapRef:
                name: ironic
          livenessProbe:
            failureThreshold: 30
          readinessProbe:
            failureThreshold: 30
        # ironic-log-watch
        - name: ironic-log-watch
          envFrom:
            - configMapRef:
                name: ironic
        # ironic-httpd
        - name: ironic-httpd
          securityContext:
            runAsUser: 0
          envFrom:
            - configMapRef:
                name: ironic
          livenessProbe:
            failureThreshold: 30
          readinessProbe:
            failureThreshold: 30
      volumes:
        - name: ironic-data-volume
          persistentVolumeClaim:
            claimName: image-pvc
