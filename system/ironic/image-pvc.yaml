apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: image-pvc
  namespace: metal3
spec:
  storageClassName: microk8s-hostpath
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
