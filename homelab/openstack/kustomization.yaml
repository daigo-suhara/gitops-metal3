apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Single ApplicationSet generates all openstack service Applications.
# Chart wrappers (with helmChart + patches) live under charts/<name>/.
resources:
  - applicationset.yaml
