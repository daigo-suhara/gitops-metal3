apiVersion: v1
kind: Service
metadata:
  name: ks-console-lb
  namespace: kubesphere-system
  labels:
    app: ks-console
spec:
  type: LoadBalancer
  selector:
    app: ks-console
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8000
